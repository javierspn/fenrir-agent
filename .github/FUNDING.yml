# Funding links — GitHub renders a "Sponsor ❤" button from the first active key below.
#
# Donations go straight to COMPUTE for the experiment:
#   • frontier-teacher API credits (faster learning while the skill library is thin)
#   • local-inference hardware — a Mac Mini lets the small-model loop run more, cheaper,
#     and lean off the paid teacher (which is literally the metric being measured).
# See the README "Support the experiment" section.
#
# Uncomment a line ONCE the account exists. Commented keys render no button.

ko_fi: javierspn                    # ko-fi.com/javierspn — instant, no approval; "Mac Mini fund" goal
# github: javierspn                 # needs GitHub Sponsors enrolled (github.com/sponsors) + Stripe Connect
# open_collective: REPLACE_ME       # transparent public ledger (money → API credits / hardware); needs a fiscal host
# buy_me_a_coffee: REPLACE_ME
# liberapay: REPLACE_ME
# custom: ["https://www.amazon.es/hz/wishlist/REPLACE_ME"]   # Mac Mini hardware wishlist
