# Tasks: Family problem generator + by-family reuse verdict (006)

**Feature**: `006-family-problem-generator` | **Spec**: [spec.md](spec.md) | **Plan**: [plan.md](plan.md)

Tests-first per story. `[P]` = parallelizable. Deploy/<host> tasks split out at the end (need live prod
+ human confirm, XII). Reuse: `fenrir.verify.sympy_oracle`, `fenrir.sandbox.runner.run` (X),
`fenrir.memory.embed`, `benchmark_loader/load.py` patterns.

---

## Phase 1: Setup

- [X] T001 [P] Create `benchmark_loader/families.py`: `FAMILIES` (10 (name,desc) tuples) + `FAMILY_POOL` (whole-family → training/evaluation/transfer, ≥1 transfer e.g. `distance-meet`, III) + `pool_for(family)` (raises on unknown). (FR-002/FR-005)
- [X] T002 [P] Write additive migration `infra/migrations/0009_benchmark_family.sql`: `benchmark_tasks.family text` + index `bench_pool_family (pool, family)`. One tx, idempotent. Do NOT apply (deploy T021).

**Checkpoint**: family taxonomy + schema defined.

---

## Phase 2: Foundational

- [X] T003 Port the Desktop generator to `benchmark_loader/generate.py`: families from `families.py`, lmstudio/ollama backends, host-side gate (restricted-builtins+SIGALRM), JSONL out — **extended to emit `solution_code`** (R8). Carries over the **per-family attempt cap + shortfall report** (U2) and **non-JSON/fenced tolerance** (strip; unparseable → skip, not fatal, U3). `--cross-check` flag stub.
- [X] T004 Create `benchmark_loader/load_generated.py` skeleton: `load_generated(conn, path) -> LoadResult{read,loaded,rejected,duplicates}`, parse JSONL, no writes yet.
- [X] T005 [P] Test scaffolds: `tests/unit/test_generate_gate.py`, `tests/integration/test_load_generated.py` (migrated_conn + sandbox-stubbed).

**Checkpoint**: modules importable; generator runs on a host; loader callable.

---

## Phase 3: User Story 1 — Clean family-structured pool exists (P1) 🎯 MVP

- [X] T006 [P] [US1] Unit: host gate rejects each unclean class — crash, forbidden token, decimal/irrational, hidden constant (q-num∉code), leaked answer, **and non-JSON/fenced output (strip→parse; unparseable→skip)** (FR-003, U3, SC-003). *(Validates the ported gate — not fail-first TDD since generate.py is a port.)*
- [X] T007 [P] [US1] Unit: `problem_id = 'qwen-gen-'+sha256(normalize(question))[:16]` is stable + dedups (FR-004, R3). Must fail first.
- [X] T008 [P] [US1] Unit: `families.pool_for` is whole-family; every family in `FAMILIES` has a pool; transfer family never maps to training (FR-005, SC-005). *(Validates the families.py map — foundational, not fail-first.)*
- [X] T009 [US1] Implement `load_generated`: sandbox-derive ground truth (`sandbox.run` harness, R2/X) → confirm vs claimed via `sympy_oracle` (reject mismatch, II) → `INSERT ... ON CONFLICT (problem_id) DO NOTHING` with `benchmark='qwen-gen'`, `contamination_safe=TRUE`, `family`, `pool_for(family)`, embedding (NULL-tolerant). Records skipped if no `solution_code` (R8).
- [X] T010 [US1] Integration: load a small JSONL → all rows `qwen-gen`/`contamination_safe=TRUE`/non-null family; `ground_truth` = sandbox-derived; reload → 0 new rows (SC-001/SC-004). Sandbox stubbed to a known answer.

**Checkpoint**: a clean, family-labelled, idempotent pool loads.

---

## Phase 4: User Story 2 — Generator ≠ judge independence (P1)

- [X] T011 [P] [US2] Unit/integration: a record whose **claimed answer disagrees** with the sandbox-derived answer is **rejected** (not loaded) (FR-001/FR-007, SC-002). Must fail first.
- [X] T012 [US2] Assert in `load_generated` that model-generated code executes ONLY via `sandbox.run` (no in-process `exec`); test asserts the sandbox path is used (FR-008, SC-008, X). **Also assert generator≠teacher identity** (C1, FR-007 AS2): the configured generation model/backend ≠ `settings.frontier_model` — a guard/test so the clean pool can't be generated by the teacher.
- [X] T013 [P] [US2] Implement `--cross-check` in `generate.py`: request a 2nd independent `solution_code`; accept only if both derive the same canonical answer (FR-011, R6). Off by default. Unit test on the agree/disagree branch.

**Checkpoint**: ground truth is verifier-owned + (optionally) dual-confirmed; generator never self-certifies.

---

## Phase 5: User Story 3 — By-family verdict is legible (P1)

- [X] T014 [P] [US3] Add the by-family within-position SQL (from [quickstart.md](quickstart.md)) to the repo (e.g. `dashboard/queries/by_family_verdict.sql`); verify it runs read-only via `grafana_ro` (FR-009).
- [X] T015 [US3] Add a by-family panel to `dashboard/provisioning/dashboards/learning.json`: per-family escalation (pos 1 vs ≥2) + reuse (pos ≥2); flat renders plainly (FR-009/FR-010, SC-006). Bump version.
- [X] T016 [US3] Integration: seed two members of one family, solve them, assert position + the join (`tasks.benchmark_id = benchmark_tasks.problem_id`) yield a queryable per-family verdict; a no-compounding family renders flat without error (SC-006/SC-007).

**Checkpoint**: the within-family compounding signal is queryable + legible.

---

## Phase 6: Polish

- [X] T017 [P] Document the generator + loader + family pool + by-family verdict in `OPS.md` (run on local/cloud GPU off-node, rsync, load, the verdict SQL). Reference `FUNDING.md` for the cloud-GPU option.
- [X] T018 [P] `benchmark_loader/Dockerfile` / deps: ensure `generate.py` + `load_generated.py` run (the loader needs the sandbox image available on <host>).
- [X] T019 Run the full gate: `ruff check .`, `mypy fenrir benchmark_loader`, `pytest tests/unit tests/integration -k "generate or load_generated or family"`, `spec_coverage`, `spec_snapshot --check` — all green.

---

## Deploy / <host> (live prod — human-confirmed XII; separate)

- [ ] T020 Generate a batch on the **local host or free cloud GPU** (GCP/Azure, FUNDING.md): `python -m benchmark_loader.generate --per-family 200 --out problems.jsonl` (+`--cross-check` for high trust).
- [ ] T021 [US1] Apply migration `0009` on <host> **after human confirmation** (XII); verify `benchmark_tasks.family` + index exist.
- [ ] T022 [US1] `rsync problems.jsonl` to <host> → `python -m benchmark_loader.load_generated --in problems.jsonl`; confirm SC-001/004/005 by query (qwen-gen rows, no dupes on reload, transfer family has 0 training rows). Backfill embeddings if needed.
- [ ] T023 [US3] Run by-family cohorts (`fenrir.core --run N`, the 004 curriculum draws the family pool) → read the **by-family within-position verdict**; record honestly in `BACKLOG.md` (within-family escalation drop + reuse, or flat = recorded negative). Mirror the panel to cosmos.

---

## Dependencies & order

Setup (T001–T002) → Foundational (T003–T005) → US1 (T006–T010) → US2 (T011–T013) → US3 (T014–T016)
→ Polish (T017–T019) → Deploy (T020–T023). US1 is the MVP (clean pool). US2 hardens integrity. US3
is the verdict read-back. Deploy needs everything merged + a generated batch.

## Parallel opportunities
T001 ∥ T002; T006/T007/T008 (US1 unit) parallel; T011 ∥ T013; T014 ∥ T015; T017 ∥ T018.

## FR / SC coverage
- FR-001→T009/T011; FR-002→T001/T003; FR-003→T006; FR-004→T007/T009/T010; FR-005→T008; FR-006→T009;
  FR-007→T011/T012 (incl. generator≠teacher identity); FR-008→T012; FR-009→T014/T015;
  FR-010→T015/T016; FR-011→T013; FR-012→T009/T010 (load idempotency).
- SC-001→T010; SC-002→T011; SC-003→T006; SC-004→T010; SC-005→T008/T022; SC-006→T015/T016;
  SC-007→T016/T023; SC-008→T012.

## Suggested MVP
**US1 (T001–T010)**: the clean, family-labelled, sandbox-verified, idempotent pool — the substrate.
US2 hardens it; US3 reads the verdict. Generation batch + cohorts are deploy.
